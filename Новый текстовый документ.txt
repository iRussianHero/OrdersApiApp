version: '3.9'

services:

  db:
    container_name: charp
    image: mono
    restart: always
    volumes:
      - ./OrderApiApp:/code
    working_dir: /code 
    command: dotnet run
    ports:
      - 7137:7137